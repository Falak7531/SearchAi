# Production build env. Set this to your deployed backend URL.
# The backend's CORS allow-list MUST include the origin where this
# frontend bundle is served from.
VITE_API_URL=https://api.your-domain.com
