fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0
PyYAML>=6.0
httpx>=0.27

# LangChain (loaders + splitters only — keeps deps light)
langchain-community>=0.2
langchain-text-splitters>=0.2
pypdf>=4.0

# Embeddings + LLMs
sentence-transformers>=2.7
groq>=0.9

# Vector + sparse retrieval
faiss-cpu>=1.8
qdrant-client>=1.9
rank-bm25>=0.2.2
numpy>=1.26

# Evaluation (optional)
ragas>=0.1.10
datasets>=2.19
